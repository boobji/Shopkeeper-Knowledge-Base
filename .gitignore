# Python 虚拟环境
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo

# 环境变量
.env
.env.local

# IDE
.idea/
.vscode/

# AI 助手本地目录
.workbuddy/

# 系统文件
.DS_Store
Thumbs.db

# 日志
*.log

# ---------- 运行时产物 / 临时数据 ----------
# 上传的 PDF、切片 JSON、向量数据等中间产物，体积大且可重新生成，不入库
knowledge/temp_data/
knowledge/processor/import_process/import_temp_dir/
**/temp_data/
**/import_temp_dir/

# ---------- 评测语料（版权敏感 + 体积大） ----------
# 语料 PDF 版权归厂商所有，且上游仓库无开源许可证：只本地使用，不入库。
# 需要时用 `python scripts/fetch_eval_corpus.py` 重新拉取（清单 manifest.csv 会一并生成）。
eval/corpus/*
!eval/corpus/README.md

# ---------- 备份文件 ----------
*_bak.py
*.bak

# ---------- 模型权重 / 缓存 ----------
# 体积巨大，应由各自环境按需下载
models/
model_cache/
*.safetensors
*.bin

# ---------- 环境变量模板需要保留 ----------
!.env.example
# ZCode 本地会话配置
.zcode/

# ---------- 运行时产物（补充） ----------
# 跨文档样板段指纹库（运行时自动生成/追加，可重建）
**/boilerplate_fingerprints.json
# 切片体检报告（diagnose_chunks.py 输出）
eval/reports/
